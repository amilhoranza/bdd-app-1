<h1>Homepage</h1>
   <% if user_signed_in? %>
    <%= link_to("Log out", destroy_user_session_path, method: :delete) %>
  <% end %>

       <p class="notice"><%= notice %></p>
     <p class="alert"><%= alert %></p>

